diff --git a/src/pi-rpc/process.ts b/src/pi-rpc/process.ts
index 937043c..276bcbc 100644
--- a/src/pi-rpc/process.ts
+++ b/src/pi-rpc/process.ts
@@ -29,5 +29,6 @@ function stripAnsi(s: string): string {
 
 type PiRpcCommand =
   | { type: 'prompt'; id?: string; message: string; images?: unknown[] }
+  | { type: 'steer'; id?: string; message: string; images?: unknown[] }
   | { type: 'abort'; id?: string }
   | { type: 'get_state'; id?: string }
@@ -208,6 +209,11 @@ export class PiRpcProcess {
     if (!res.success) throw new Error(`pi prompt failed: ${res.error ?? JSON.stringify(res.data)}`)
   }
 
+  async steer(message: string, images: unknown[] = []): Promise<void> {
+    const res = await this.request({ type: 'steer', message, images })
+    if (!res.success) throw new Error(`pi steer failed: ${res.error ?? JSON.stringify(res.data)}`)
+  }
+
   async abort(): Promise<void> {
     const res = await this.request({ type: 'abort' })
     if (!res.success) throw new Error(`pi abort failed: ${res.error ?? JSON.stringify(res.data)}`)
diff --git a/src/acp/agent.ts b/src/acp/agent.ts
index 93d1c01..900cb3e 100644
--- a/src/acp/agent.ts
+++ b/src/acp/agent.ts
@@ -440,6 +440,16 @@ export class PiAcpAgent implements Agent {
 
     const { message, images } = promptToPiMessage(params.prompt)
 
+    // PISS uses ACP metadata rather than Pi-specific control-plane logic. Pi's
+    // adapter translates the harness-neutral delivery intent to Pi RPC.
+    const delivery = (params as PromptRequest & {
+      _meta?: { piss?: { delivery?: string } }
+    })._meta?.piss?.delivery
+    if (delivery === 'steer') {
+      await session.proc.steer(message, images)
+      return { stopReason: 'end_turn' }
+    }
+
     // Built-in ACP slash command handling (headless-friendly subset).
     if (images.length === 0 && message.trimStart().startsWith('/')) {
       const trimmed = message.trim()
