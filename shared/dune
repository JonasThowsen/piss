; Pure type and protocol definitions for the Piss native services.
;
; Anything that depends on IO belongs in an ownership library under src/lib/
; (`piss.persistence`, `piss.worker-store`, `piss.registry`, or
; `piss.workspace-io`), not here. The separately compiled browser implements
; equivalent decoders at the HTTP boundary.

(library
 (name piss_shared)
 (public_name piss.shared)
 (libraries yojson)
 (wrapped true))
