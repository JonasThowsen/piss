; Pure type and protocol definitions for the PISS native services.
;
; Anything that depends on IO (filesystem access, SQLite stores,
; network requests) belongs in src/lib/ as part of `piss.core`,
; not here. The separately compiled browser implements equivalent
; decoders at the HTTP boundary.

(library
 (name piss_shared)
 (public_name piss.shared)
 (libraries yojson)
 (wrapped true))
